# Copy to profiles.yml (gitignored) and adjust paths for your environment.

default:
  target: duckdb
  database: main
  schema: main
  duckdb_path: demo.duckdb

acme_prod:
  target: snowflake
  database: ANALYTICS
  schema: ML_STAGING

# ADR-003: CLI-first interface

**Status:** Accepted

**Context:** CLI vs library-first vs web UI.

**Decision:** CLI-first (`trustline` command). Python library API in v0.2.

**Rationale:** CI/CD friendly; terminal demo; natural GitHub Actions integration.

**Consequences:** No web UI until v1.0.

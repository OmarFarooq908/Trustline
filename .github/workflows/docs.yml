# MkDocs deploy — disabled until site config is added
#
# Planned: MkDocs Material build + GitHub Pages deploy

name: Docs

on:
  workflow_dispatch:

jobs:
  docs:
    name: Docs
    runs-on: ubuntu-latest
    if: false  # Enable when MkDocs is configured
    steps:
      - uses: actions/checkout@v7
      - run: echo "MkDocs deploy not yet configured"
